docker rm cfimager-server
docker rm cfimager-mailer-api
docker rm cfimager-mailer
docker rm cfimager-compilers-spawner
docker rm cfimager-mariadb
docker network rm cfimager-database
docker network rm cfimager-mailer-api
docker network rm cfimager-mailer
docker network rm cfimager-compilers
docker network rm cfimager-compilers-api